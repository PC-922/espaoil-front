# Proxy target for the Vite development server.
# The /api route is forwarded to this URL.
# Use http://localhost:8080 for a local backend, or https://your-domain.com for a remote one.
VITE_API_PROXY_TARGET=http://localhost:8080
